// What i want
// Upload and send
// User can enter number, name and template to send automatic whatsapp.
// User can upload file and template, and all of those numbers will be whatsapped to
// Once any number and name has been whatsapped, we should save their name and whatsapp number in a table

// STEPS
// get clinic, nextecomm project up

// Header
// - import, but just leave it blank for now

// Home page
// - allow users to choose if its individual or bulk. By default, it is individual.
// For individual, they need to fill up a form of - name, number without the +6, and message. Once they input this, it goes to backend which we will deal with later
// For bulk, allow users to upload a csv file, and messsage, then it gets sent to backend. we will deal with this later
